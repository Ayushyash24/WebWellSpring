// Firebase removed — using local auth instead
// This file is kept as a stub to avoid import errors if any file still imports from here

export const auth = null;
export const db = null;
export default null;
